name = "ern-site"
main = "cloudflare-worker/src/index.js"
compatibility_date = "2025-12-02"

[vars]
GITHUB_OWNER = "amtbrs-03"
GITHUB_REPO = "AMTBRS"
GITHUB_BRANCH = "site-release"
TO_EMAIL = "amtbrs@icloud.com"
FROM_EMAIL = "no-reply@ern-cicek.com.tr"

# Optional (recommended): Unicode-capable TTF font for PDF invoices
# - Put as a secret to avoid committing large base64 to the repo:
#   wrangler secret put INVOICE_TTF_BASE64
# - Value: base64-encoded .ttf (you can paste output of `base64`)
